{
    "name": "sillytavern-openai-responses",
    "version": "0.2.0",
    "description": "OpenAI Responses API source for SillyTavern",
    "type": "module",
    "main": "server/index.js",
    "license": "AGPL-3.0-only",
    "scripts": {
        "test": "node --test",
        "check": "node --check index.js && node --check server/index.js && node --check server/adapter.js && node --check server/proxy.js"
    },
    "engines": {
        "node": ">=20"
    },
    "peerDependencies": {
        "node-fetch": "^3.3.2",
        "proxy-agent": ">=6.5.0 <9"
    }
}
