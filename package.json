{
    "name": "sillytavern-openai-responses",
    "version": "0.1.0",
    "description": "OpenAI Responses API source for SillyTavern",
    "type": "module",
    "main": "server/index.js",
    "license": "AGPL-3.0-only",
    "scripts": {
        "test": "node --test",
        "check": "node --check index.js && node --check server/index.js && node --check server/adapter.js"
    },
    "engines": {
        "node": ">=20"
    }
}
